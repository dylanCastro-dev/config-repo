spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/productdb

server:
  port: 8081

logging:
  level:
    root: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  api-docs:
    enabled: true