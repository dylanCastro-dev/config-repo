spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/transactiondb

server:
  port: 8082

logging:
  level:
    root: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  api-docs:
    enabled: true