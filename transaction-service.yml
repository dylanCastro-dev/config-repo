spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/transactiondb

server:
  port: 8082

logging:
  level:
    root: INFO

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  api-docs:
    path: /v3/api-docs